# back

> A Node.js project

## Build Setup

* NPM

``` bash
# install dependencies
npm install

# serve at localhost:3000
npm start
```

* YARN

``` bash
# install dependencies
yarn install

# serve at localhost:3000
yarn start
```
